package com.invocation.server.repository;

import com.invocation.server.entity.SupportCenter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportCenterRepo extends JpaRepository<SupportCenter, Integer> {}
